{
  "name": "vue-vite-template",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "clean": "rimraf dist",
    "reset": "rimraf dist && rimraf node_modules"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "vite": "^2.8.4",
    "vite-rust-plugin": "^1.0.0"
  }
}
